{
  "name": "@cartograph/server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --format=esm --target=node20 --outfile=../skills/cartograph/server.mjs --banner:js='#!/usr/bin/env node'",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.6",
    "hono": "^4.10.7"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "esbuild": "^0.27.2",
    "typescript": "~6.0.2"
  }
}
