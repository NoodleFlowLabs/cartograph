# Cartograph UI Runtime

This directory is generated from the repo-root `ui` workspace.

Run this from the project root:

```bash
node skills/cartograph/server.mjs
```

The server binds to `127.0.0.1`, serves the bundled React UI, and exposes purpose-built Cartograph file endpoints.
